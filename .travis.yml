language: python
python:
  - '3.8.5'
services:
  - postgresql
before_script:
  - pip install -r requirements.txt


env:
  DJANGO_SETTINGS_MODULE="purbeurre.settings.travis"

script:
  - pytest -v

# safelist
branches:
  only:
    - staging
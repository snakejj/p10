language: python
python:
  - '3.5'

before_script:
  - pip install -r requirements.txt

env: DJANGO_SETTINGS_MODULE="purbeurre.settings"

script:
  - ./manage.py test

# safelist
branches:
  only:
    - staging